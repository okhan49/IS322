import React from 'react';
import PropTypes from 'prop-types';

const HomeScreen = () => {
    return (
        <button type="button"
         className="playGame">Play Game</button>
    );
};

export default HomeScreen;